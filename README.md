rails-nginx-docker
==================
### Install docker
`https://docs.docker.com/installation/mac/`

### Mac 启动需要
`boot2docker start`

### Install fig
`brew install fig`

### build
`fig build`

### run
`fig up`